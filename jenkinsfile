pipeline{
    agent any
    environment{
        staging_server="192.168.25.189"
    }
    stages{
        stage('Deploy to Remote'){
            steps{
                sh '''
                        scp -r ${WORKSPACE}${fil} root@${staging_server}:/var/www/html/php${fil}
                    done
                '''
            }
        }
    }
}
